#
#	startup configuration for plog server
#	created : ${${PROJECT_NAME}_NOW_UTC}
#	platform: @CMAKE_SYSTEM@
#

[plog]
json = /etc/opt/smf/plog_v${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.json 
pool-size = ${${PROJECT_NAME}_POOL_SIZE}
