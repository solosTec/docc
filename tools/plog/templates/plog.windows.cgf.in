#
#	startup configuration for @PROJECT_NAME@ ipt node
#	created : ${${PROJECT_NAME}_NOW_UTC}
#	platform: @CMAKE_SYSTEM@
#

[service]
enabled = false
name = plog_v${${PROJECT_NAME}_VERSION_MAJOR}

[plog]
json = $ENV{ProgramData}\solosTec\SMF\plog_v${${PROJECT_NAME}_VERSION_MAJOR}.${${PROJECT_NAME}_VERSION_MINOR}.json
pool-size = ${${PROJECT_NAME}_POOL_SIZE}
